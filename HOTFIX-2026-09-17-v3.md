# 이상위키 자동배포 핫픽스 v3

## 수정 내용
- 원본 Google Docs의 첫 대형 표가 `목차(왼쪽) + 팀 정보표(오른쪽)`를 한 표로 묶고 있는 구조를 인식합니다.
- 기존 빌더가 이 4열 표 전체를 정보표로 오인해 목차가 남색 정보표로 한 번 더 출력되던 문제를 수정했습니다.
- 이제 `팀명 / 업종명 / 설립일 / 팀장 / 관리자 / ...` 라벨이 있는 열을 자동 탐지해 그 열과 값 열만 정보표로 추출합니다.
- 정보표의 빈 스페이서 행은 출력하지 않습니다.
- 제목 행과 로고 행의 colspan 처리를 보정했습니다.

## 교체 파일
`scripts/build_site.py` 하나만 GitHub 저장소의 동일 경로에 덮어쓴 뒤 Commit하고 Actions를 재실행하세요.
