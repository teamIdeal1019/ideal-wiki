# 2026-09-17 공개 필터 / 표 / 아이콘 수정

이번 수정은 첫 직접 배포에서 확인된 세 가지 문제를 고칩니다.

1. `10.2. 학생들`은 제목/링크만 지우지 않고, 그 문단에 속한 **표·이미지·본문 전체를 다음 동급/상위 제목까지 제거**합니다.
2. 안전장치로 `기수 / 활동명 / 한 줄 메시지` 헤더를 가진 학생 명단 표를 독립적으로 탐지해 통째로 제거하며, 공개 검증기에서도 같은 표가 남으면 배포를 실패시킵니다.
3. Google Docs API에서 inline image의 표시 크기를 보존해 링크 아이콘이 원본 비트맵 크기로 확대되지 않게 했습니다. 작은 인라인 이미지는 `doc-inline-icon`으로 추가 제한됩니다.
4. Docs API 변환 과정에서 빠졌던 표 헤더 배경색을 팀 이상 네이비로 고정하고 헤더 텍스트는 흰색으로 보이게 했습니다. 다크 모드에도 동일하게 적용됩니다.
5. 작품 목록의 `참여진 / 참여자 / 작업자` 열은 링크만 끊는 것이 아니라 열 자체를 제거합니다. 헤더 안 공백에도 대응합니다.

기존 GitHub 저장소에서는 다음 네 파일만 교체하면 됩니다.

- `scripts/build_site.py`
- `scripts/validate_public.py`
- `config/public_rules.json`
- `site/assets/wiki.css`

교체 후 Actions에서 `Sync Ideal Wiki from master Google Doc`을 다시 실행하세요.
